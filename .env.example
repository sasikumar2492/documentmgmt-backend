# Database (use your existing names; add host/port if needed)
DATABASENAME=pharma_dms_test
DATABASEUSER=postgres
DATABASEPASSWORD=postgres
DATABASEHOST=localhost
DATABASEPORT=5432

# Server
PORT=4000

# Auth
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Local file storage (relative to backend folder or absolute path)
FILE_STORAGE_PATH=./uploads

# Syncfusion Word Processor Server (for Original Doc Word preview). Run: docker-compose -f docker-compose.syncfusion.yml up -d
DOCUMENT_EDITOR_SERVICE_URL=http://localhost:6002
